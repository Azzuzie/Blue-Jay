Hi i am Ajith,
1. First install node modules using command " npm install "
2. After that start the server " node server.js " 
3.The server will starts running at port 4000
4. After that open mongodb compass and open Interntask database and inside it  u will find data collection name ,
	press import and import the " data.csv " file 
5. Now open postman and start working with apis
 		{
			method: GET
			url       : http://localhost:4000/consecutive-employees
		}

		{
			method: GET
			url       : http://localhost:4000/shift-conditions
		}
